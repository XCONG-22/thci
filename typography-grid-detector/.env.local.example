# 若编辑器不便打开 .env.local，请改用同目录下的 api-credentials.env（记事本可编辑），
# 见 api-credentials.env.example；npm run dev 会通过 scripts/run-with-env.mjs 自动加载。
#
# 多模态主分析（识图 + 版式/字体初判）。留空则走「内置字体名库 ∪ 侧栏字库」本地示意模式。
OPENAI_API_KEY=

# 视觉分析模型（默认 gpt-4o）
# OPENAI_MODEL=gpt-4o

# 在初判之后，用轻量模型从「每块 Top-K 候选」中择定族名（默认开启）。关闭填 0 / false
# OPENAI_REFINE_FONTS=true
# OPENAI_REFINE_MODEL=gpt-4o-mini

# 开发模式局域网访问：用手机/另一台电脑打开时若 Next 报跨域，写入浏览器地址栏里的 IP（不要带 :端口），逗号分隔
# NEXT_DEV_ALLOWED_ORIGINS=10.8.232.157

# 若直连 api.openai.com 出现 403 unsupported_country_region_territory：
# 说明你当前网络/账单地区不在 OpenAI 支持范围内。可选方案（须自行确保合规）：
# 1）使用所在地区合规、且提供 OpenAI 兼容 API 的服务商，在其控制台获取密钥与 Base URL；
# 2）Azure OpenAI / 企业代理等（路径可能与 /v1 不同，按服务商文档填写）。
# 下面两项仅在走「兼容 OpenAI Chat Completions」的网关时需要修改，默认不用动。
# OPENAI_API_BASE=https://api.openai.com/v1
