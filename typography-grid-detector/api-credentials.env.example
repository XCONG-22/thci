# 使用说明：
# 1) 把本文件复制一份，改名为 api-credentials.env（与 package.json 同目录）
# 2) 用「记事本」打开 api-credentials.env，在等号后面粘贴密钥，保存为 UTF-8
# 3) api-credentials.env 已在 .gitignore 中，勿上传到公开仓库
#
# 也可继续用 .env.local（若编辑器支持）；两者同时存在时 .env.local 会覆盖同名变量

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
OPENAI_REFINE_MODEL=gpt-4o-mini
